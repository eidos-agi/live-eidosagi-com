---
id: TASK-0011
title: Upgrade OG image + share artifact system
status: To Do
created: '2026-04-17'
priority: High
milestone: Phase 2 — Self-Portrait Loop
---
Current /og.png is a typographic placeholder generated by scripts/gen-og.py. Need a real hero asset + a dynamic per-run share card.

**A. Hero OG (replaces current /og.png)**
- 1200x630. Workshop palette. Three GPU lanes rendered as stylized horizontal bars with lit amber endpoints (fire metaphor). Typography: "CRUCIBLE" + tagline "We put models in the fire." + the H100 headline stat ("$5.46/M tokens — 4x cheaper per token than any prototyping tier"). Subtle grain overlay.
- Tool: python + Pillow, replace gen-og.py. Commit the PNG output.

**B. Dynamic per-run share card**
- `/api/share/[runId]` already exists (scaffolded). Upgrade to render podium + numbers + per-run model name + timestamp. Use next/og ImageResponse. Make sure Twitter + LinkedIn + Open Graph previews look beautiful.

**C. Per-page OG overrides**
- `/research/why-local-matters`, `/about`, `/methodology`, `/models` each get their own OG variant. Template, not one-offs.

**D. Share-card CTA on every run/runs page + hero**
- "Share this run" button next to the run header — copies the per-run URL + opens tweet-intent.
